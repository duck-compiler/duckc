use std::cmd::{Cmd};
use std::{assert};
use std::error::panic;

fn main() {
    let output = Cmd::exec("ls -la")
        .expect("this should work");

    assert(output.stdout.contains(".dargo"), "ls -la output doesn't contain .dargo");
}
