use std::col::{GoMap};
use std::io::{println};

fn main() {
    let m = GoMap::new<String, Int>();

    m.set("A", 22);
    m.set("B", 23);
    m.set("D", 100);

    m.get(&"A").unwrap().to_string()->println();

    let x = m.get_mut(&"A").unwrap();
    *x = 10;

    m.get(&"A").unwrap().to_string()->println();
}
