use std::io::println;
use std::password::BCrypt;

fn main() {
    const b = BCrypt::new();
    const original = "secret password 😎";

    const h = b.hash(original).unwrap();

    b.verify(original, h).expect("this should not fail");
    println("secret password 😎 ok");

    b.verify("123", h).expect_err("this should fail");
    println("123 not ok");
}
