use std::io::{println};

type User<T: {
    name: String,
    age: Int,
}> = T;

type UserWithOther = User<{
    name: String,
    age: Int,
    other: String,
}>;

fn main() {
    println("correct")
}
