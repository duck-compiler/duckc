struct List<T> {
    elems: T[],
} impl {
    fn m<U>(f: fn(v: T) -> U) {}
}

fn main() {
    let list: List<String> = List<String> { elems: [] as String[] };
    list.m<Int>(fn(v: String) -> Int { return 200; });
}
