use std::io::{println};
use std::string::{from_int};

fn main() {
    for (i in [1, 2, 3].iter()) {
        const as_str = from_int(*i);

        println(f"{as_str} before break");
        const x: String = break;
        println(f"{as_str} after break");
    }
}
