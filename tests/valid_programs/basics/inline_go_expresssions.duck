fn main() {
    const msg = go {

        result_channel := make(chan string)

        go func(){
            result_channel <- "hello from the go routine inside the inline go expr"
            }()

        $ = <-result_channel
    } as String;
    msg->std::io::println();
}
