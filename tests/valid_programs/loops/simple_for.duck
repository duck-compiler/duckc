struct S {
    f: Int[],
}

fn main() {
    const s = &mut S { f: [2, 3], };

    for (i in (&mut s.f).iter_mut()) {
        std::io::println(std::string::from_int(*i));
        *i = *i * 10;
    }

    for (i in (&s.f).iter()) {
        std::io::println(std::string::from_int(*i));
    }
}
