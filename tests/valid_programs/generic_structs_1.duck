use std::io::{println};

struct M<T> = {
    name: T,
} impl {
    fn y() {
        println(self.name);
    }

    fn copy() -> Self {
        return Self { name: self.name };
    }
};

fn main() {
    let x: M<String> = M<String> { name: "Hallo" };
    x.y();
}
