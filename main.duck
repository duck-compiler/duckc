use std::io::{File};

fn main() {
    const file = match File::new("example.txt") {
        File @f => f,
        else => return,
    };

    let x = file.lines()
        .into_list();

    x.push("hello");
    std::io::debug(x);
}
