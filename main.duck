use std::io::{println};

struct M<T> = {
    name: T,
} impl {
    fn y() {
        println("hallo");
    }
};

fn main() {
    let x: M<String> = M<String> { name: "Hallo" };
    println(x.name);
    x.y();
}
