type X<A> = Y<A>;
type Y<C> = {x: C} & {y: String} & { y: String } | String | Z;
type Z = String | Int | Lol | Lol2<Int>;

type L1<X> = L2<X>;
type L2<Z> = Z;

struct Lol {}
struct Lol2<T> {
    x: T,
}

fn main() {}

fn a() -> X<Float> {}
