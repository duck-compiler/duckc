type None = .none;
type Opt<T> = T | None;

module io;
module task;
module time;
module string;
module col;
module error;

component Lol(params: {}) {
    const [x, setX] = useState(false);

    return (
        <div></div>
    )
}
