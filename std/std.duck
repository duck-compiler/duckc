use go "encoding/json";

type None = .none;
type Opt<T> = T | None;

module io;
module task;
module time;
module string;
module col;
module error;
module http;
module sync;

component lol() tsx {
    return (<><p>yo</p></>);
}

fn parse_json<T: {}>(input: String) -> T | .err {
    go {
        return <<<@T>>>_FromJson(input);
    }
}
