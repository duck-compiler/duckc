use go "fmt";
fn main() {
    let x: struct {x: {x: Int}} = .{x: { x: 10 }};
    let y: struct {x: {x: struct { x: Int }}} = .{x: { x: .{x: 10} }};
    go {
        fmt.Println(x)
        fmt.Println(y)
    }
}
