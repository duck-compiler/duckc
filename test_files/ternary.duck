use go "fmt";

fun main() {
    let x: Int = 1;

    let y: Bool = if (x == 1) { true } else { false };
    let z: String = if (x == 2) { if (true) { "yo" } else { "not yo" } } else { if (1 == 1) { if (1 == 1) {

    go {
        fmt.Println("go in between lulz")
    }

    "1 is 1"

    } else { "1 is 1 but 1 is not 1" } } else { "1 is not 1??" } };


    if ({x: 10}.x == 10) {
        go {
            fmt.Println("is 10")
        }
    }

    go {
        fmt.Println(y)
        fmt.Println(z)
    }
}
