use go "fmt";
fn main() {
    let x: (Int, String, (Int, Int)) = (1, "hallo", (30, 50));
    let y: (Int, { x: String }, (Int, Int)) = (1, { x: "hallo" }, (30, 50));
    let xxx: (Int, { x: String, y: (Int, struct {x: String}) }, (Int, Int))
    = (1, { x: "hallo", y: (100, .{ x: "yo moin" }) }, (30, 50));
    let zz: String = y.1.x;
    let zzz: String = xxx.1.y.1.x;
    go {
        fmt.Println(zz, zzz)
    }
}
