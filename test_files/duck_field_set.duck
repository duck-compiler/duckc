use go "fmt";

fn main() {
    let x: { y: String } = { y: "hallo" };
    let y: (Int, String) = (10, "abc");

    go {
        fmt.Println(x)
        fmt.Println(y)
    }

    x.y = "kek";
    y.0 = 100;

    go {
        fmt.Println(x)
        fmt.Println(y)
    }

    let xx: Int = 10;
    xx = 20;

    let z: (Int, { x: String }) = (10, { x: "Hallo" });
    z.1.x = "ööö";
    go {
        fmt.Println(z)
        fmt.Println(z.field_1.Getx())
    }

}
