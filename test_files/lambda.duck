use go "fmt";

fun main() {
    let a: Int = ((x: Int) -> Int => {
        go {
            fmt.Println(x)
        }
        return 1;
    })(1);

    let b: Int = ((x: Int) -> Int => {
        go {
            fmt.Println(x)
        }
        return 1;
    })(1);

    let c: () -> go any = () -> go any => {};

    go {
        fmt.Println("Got from lambda", a, b)
    }
}

fun cc() {}

fun abc(x: {z: String}) {

}
