use go "fmt";

fn main() {
    let a: Int = (fn(x: Int) -> Int => {
        go {
            fmt.Println(x)
        }
        return 1;
    })(1);

    let b: Int = (fn(x: Int) -> Int => {
        go {
            fmt.Println(x)
        }
        return 1;
    })(1);

    let c: fn() -> go any = fn() -> go any => {};

    c();

    go {
        fmt.Println("Got from lambda", a, b)
    }
}

fn cc() {}

fn abc(x: {z: String}) {

}
