use go "fmt";

fun main() {
    let a: Int = ((x: Int) -> Int => {
        go {
            fmt.Println(x)
        }
        return 1;
    })(1);

    let b: Int = ((x: Int) -> Int => {
        go {
            fmt.Println(x)
        }
        return 1;
    })(1);

    go {
        fmt.Println("Got from lambda", a, b)
    }
}

fun abc(x: {z: String}) {

}
