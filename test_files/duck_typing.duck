type Alias = { x: String };

fn yolo(x: { x: String }) {
    std::io::println(x.x);
}

fn a() {
    while (true) {
        std::io::println("yo");
    }
}

fn main() {
    std::task::spawn(a);
    let y: Alias = { x: "Hallo, Welt" };
    while (true) {
        yolo(y);
    }
}
