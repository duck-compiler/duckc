type MyDuck = duck {
    to_string: (self) -> String => {
        console.log(self);
    }
}

MyDuck

fn gen_str(x: { to_string: fn() -> String | Option<String> }) {
    **()* ()*()()*SDAF()*F
}

fn main() {
    const x: {} = {};
    gen<typeof x>(x);
}
