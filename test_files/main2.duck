use go "fmt";
use go "strings";

fn fprintln(input: String, args: String[]) -> String {
    go {
        result := input.as_dgo_string()
        for _, arg := range args {
            arg := arg.as_dgo_string()
            result = strings.Replace(result, "{}", arg, 1)
        }
        fmt.Println(result);

        return ConcDuckString { value: result };
    }

    return ""
}

fn x() -> String | Bool {
    return "hallo";
}

fn main() {
    let zzz: Int = "abc";
    x();
    fprintln("Hello, World", .String[]);
}
