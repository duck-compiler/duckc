use std::web;
use std::io::{println};

fn main() {
    let x: String | .err = "hallo";

    std::web::serve_string("/", "hello, world");
    println("listening on port 8080");
    let http_server = std::web::listen(3000);
    let http_server = std::web::listen(3000);
}
